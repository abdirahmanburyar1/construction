# Neon or any PostgreSQL - use pooled URL for serverless
DATABASE_URL="postgresql://user:password@host/neondb?connect_timeout=15&sslmode=require"

# dhisme.so (prod); localhost:3000 (dev)
PLATFORM_DOMAIN=dhisme.so
ADMIN_EMAIL=admin@platform.com
ADMIN_PASSWORD=changeme
